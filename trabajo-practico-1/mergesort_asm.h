#ifndef MERGE_SORT_H
#define MERGE_SORT_H

#include <stdlib.h>

// Receives a pointer to the array to be sorted (p)
// and an integer with the size of the array (size).
// Sorts the array modifing the original one.
extern void merge_sort(int p[], size_t size);

#endif
